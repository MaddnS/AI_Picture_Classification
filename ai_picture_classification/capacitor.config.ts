import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ai_picture_classification',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
